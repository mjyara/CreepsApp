import { SizeformatpipePipe } from './sizeformatpipe.pipe';

describe('SizeformatpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SizeformatpipePipe();
    expect(pipe).toBeTruthy();
  });
});
