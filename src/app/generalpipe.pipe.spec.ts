import { GeneralpipePipe } from './generalpipe.pipe';

describe('GeneralpipePipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralpipePipe();
    expect(pipe).toBeTruthy();
  });
});
