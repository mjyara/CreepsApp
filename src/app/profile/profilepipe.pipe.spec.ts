import { ProfilepipePipe } from './profilepipe.pipe';

describe('ProfilepipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilepipePipe();
    expect(pipe).toBeTruthy();
  });
});
